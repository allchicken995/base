<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ronllan.modules.sys.dao.SysUserDao">
	
	<resultMap id="SysUserEntity" type="com.ronllan.modules.sys.entity.SysUserEntity"></resultMap>

	<select id="getObjectList" resultMap="SysUserEntity">
		select 
			sysUser.*, 
			sysDept.name as deptName
		from 
			sys_user sysUser 
			left join sys_dept sysDept on sysDept.id = sysUser.dept_id
				and sysDept.logical_delete = 0
		where 
			sysUser.super_admin = 0
		<if test="username != null and username.trim() != ''">
			and sysUser.username like #{username}
		</if>
		<if test="deptId != null and deptId.trim() != ''">
			and sysUser.dept_id = #{deptId}
		</if>
		<if test="deptIdList != null">
			and sysUser.dept_id in
			<foreach item="id" collection="deptIdList" open="(" separator="," close=")">
				#{id}
			</foreach>
		</if>
	</select>

</mapper>