<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ronllan.modules.security.dao.SysUserTokenDao">

	<resultMap id="SysUserTokenEntity" type="com.ronllan.modules.security.entity.SysUserTokenEntity"></resultMap>

	<update id="logout">
		update 
			sys_user_token 
		set 
			expire_date = #{expireDate} 
		where 
			user_id = #{userId}
		and logical_delete = 0
	</update>

	<select id="getOnlineList" resultMap="SysUserTokenEntity">
		select 
			sysUserToken.user_id,
		    sysUserToken.expire_date,
		    sysUserToken.update_date as login_date,
		    sysUser.username,
		    sysUser.real_name
		from 
			sys_user_token sysUserToken 
			inner join sys_user sysUser on sysUser.id = sysUserToken.user_id
				and sysUser.logical_delete = 0
		where 
			sysUserToken.logical_delete = 0
		and	sysUserToken.expire_date > #{expireDate}
		<if test="username != null and username.trim() != ''">
			and sysUser.username like concat(concat('%',#{username}),'%')
		</if>
	</select>
</mapper>