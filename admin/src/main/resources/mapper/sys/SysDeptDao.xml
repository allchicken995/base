<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ronllan.modules.sys.dao.SysDeptDao">

	<resultMap id="SysDeptEntity" type="com.ronllan.modules.sys.entity.SysDeptEntity"></resultMap>
    <parameterMap id="SysDeptEntity" type="com.ronllan.modules.sys.entity.SysDeptEntity"></parameterMap>

	<select id="getObjectList" resultMap="SysDeptEntity">
		select 
			sysDept.*
			,parent.name as parentName
		from
			sys_dept sysDept
		left join sys_dept parent on sysDept.pid = parent.id and parent.logical_delete = 0
		where
			sysDept.logical_delete = 0
		<if test="deptIdList != null">
			and	sysDept.id in
			<foreach item="id" collection="deptIdList" open="(" separator="," close=")">
				#{id}
			</foreach>
		</if>
	</select>

	<select id="getObjectById" resultMap="SysDeptEntity">
		select 
			sysDept.*
			,parent.name as parentName
		from
			sys_dept sysDept
		left join sys_dept parent on sysDept.pid = parent.id and parent.logical_delete = 0
		where
			sysDept.logical_delete = 0
		and sysDept.id = #{id}
	</select>

</mapper>